import { Routes } from '@angular/router';

export const routes: Routes = [
    // { path: 'lineup', loadComponent: () => import('./lineup-page/lineup-page.component').then(m => m.LineupPageComponent) },
];
